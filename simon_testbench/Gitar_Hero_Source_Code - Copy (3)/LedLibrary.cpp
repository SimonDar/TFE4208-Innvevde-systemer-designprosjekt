#include "LedLibrary.hpp"

int prosentage(int HighNumber, int LowNumber) {
    
    int results = 1;
/*
    float Input = float(CheckInput());
    float MaxValueOut = 100;
    float MinValueOut = 0;

    float coeff[3][4] = {
        { float(pow(Input, 2)), Input, 1, 0 },
        { float(pow(LowNumber, 2)), LowNumber, 1, 100 },
        { float(pow(HighNumber, 2)), HighNumber, 1, 100 },
    };



    vector<float> Formula = findSolution(coeff);
    */
    return(results);
}


